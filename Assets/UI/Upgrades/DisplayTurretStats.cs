using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DisplayTurretStats : MonoBehaviour
{
    /*
    public TurretStats turretStats;
    public Text statsText;
    private bool displayOn = false;
    private bool buttonClicked = false;

    private void Start()
    {
        gameObject.SetActive(displayOn);
        
        if (turretStats == null)
        {
            Debug.LogError("TurretStats component not assigned!");
            return;
        }
        if (statsText == null)
        {
            Debug.LogError("Stats Text UI not assigned!");
            return;
        }
        
        DisplayStats();
    }

    public void ShowStats()
    {
        if (displayOn)
        {
            DisplayStats();
            buttonClicked = false;
        }
        else
        {
            
        }
    }
    
    private void DisplayStats()
    {
        statsText.text = "--- Turret Stats ---\n" +
                         $"Total Upgrades: {turretStats.totalAmountOfUpgrades}\n" +
                         $"Fire Rate Modifier: {turretStats.modifierFireRate}\n" +
                         $"Bullet Lifetime Modifier: {turretStats.modifierBulletLifeTime}\n" +
                         $"Bullet Speed Modifier: {turretStats.modifierBulletSpeed}\n" +
                         $"Is Homing: {turretStats.isTurretHoming}\n" +
                         $"Pierce Count: {turretStats.pierceCount}\n" +
                         $"Angle Spread: {turretStats.angleSpread}\n" +
                         $"Extra Projectiles: {turretStats.extraProjectiles}\n" +
                         $"Shoot 4 Projectiles: {turretStats.Shoot4Projectiles}\n" +
                         $"Multi Shot Count: {turretStats.multiShotCount}\n" +
                         $"Multi Shot Delay: {turretStats.multiShotDelay}\n" +
                         $"Allow 4 Shoot Points: {turretStats.allow4ShootPoints}\n" +
                         $"Active Directions: {turretStats.activeDirections}\n" +
                         $"Allow Diagonal Shooting: {turretStats.allowDiagonalShooting}\n" +
                         $"Amount of Bounces: {turretStats.amountOfBounces}\n" +
                         $"Allow Bullets to Bounce: {turretStats.AllowBulletsToBounce}\n" +
                         $"Chain Range: {turretStats.chainRange}\n" +
                         $"Allow Bullets to Chain: {turretStats.AllowBulletsToBouncesToChain}\n" +
                         $"Projectiles Return: {turretStats.projectilesReturn}\n" +
                         $"Enable Orbit: {turretStats.enableOrbit}\n" +
                         $"Orbit Radius: {turretStats.orbitRadius}\n" +
                         $"Orbit Speed: {turretStats.orbitSpeed}\n" +
                         $"Gold On Hit: {turretStats.GoldOnHit}\n" +
                         $"Spiral Bullets: {turretStats.spiralBullets}\n";
    }
    */
}
